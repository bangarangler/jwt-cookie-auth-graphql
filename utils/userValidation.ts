export const userValidation = (req: any) => {
  // check if there is a refreshToken
  // check if there is a accessToken
  // if neither not authenticated
  // if refreshToken or accessToken validate them
  // if valid re-make accessToken and refreshToken update redis
  // if not valid not authenticated
};
