mutation Register($input: UserRegisterInput!) {
  register(input: $input) {
    error {
      message
    }
    errors {
      source
      message
    }
    user {
      ...UserFrag
    }
    accessToken
  }
}
